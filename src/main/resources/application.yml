server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/re-book?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.rebookbackend.entity
  configuration:
    map-underscore-to-camel-case: true